name: Docker Image CI

on:
  push:
    branches:
      - devinfra


jobs:
  build:
    name: Build
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag my-cpp-image:$(date +%s)
